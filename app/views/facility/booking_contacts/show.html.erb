<h1>問い合わせ詳細</h1>
<table class="table table-bordered table-responsive-md shadow">
  <tr>
    <th class="table-active">問い合わせ状態</th>
    <td>
      <%= @booking_contact.status_i18n %>
      <% if @booking_contact.status == "awaiting_reply" %>
        <%= link_to "予約可能で返事する", facility_booking_contact_reply_path(@booking_contact), method: :patch, data: { confirm: "予約可能でお返事してよろしいですか？(送信先：#{@booking_contact.use_plan.care_manager.full_name} ケアマネージャー)" } %>
      <% end %>
    </td>
  </tr>
  <tr>
    <th class="table-active"> 利用計画状態</th>
    <td>
      <%= @booking_contact.use_plan.status_i18n %>
      <% if @booking_contact.use_plan.facility.nil? %>
        (未定)
      <% else %>
        (<%= @booking_contact.use_plan.facility.name %>)
      <% end %>
    </td>
  </tr>
  <tr>
    <th class="table-active">問い合わせ日時</th>
    <td><%= @booking_contact.created_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
  </tr>
  <tr>
    <th class="table-active">ご利用者様</th>
    <td>
      <%= @booking_contact.use_plan.user.full_name %> 様
      <% if @booking_contact.new_user? %>
        (新規)
      <% else %>
        (契約済)
      <% end %>
    </td>
  </tr>
  <tr>
    <th class="table-active">期間</th>
    <td><%= @booking_contact.use_plan.start_date %> 〜 <%= @booking_contact.use_plan.end_date %></td>
  </tr>
  <tr>
    <th class="table-active">問い合わせ元ケアマネージャー</th>
    <td><%= @booking_contact.use_plan.care_manager.full_name %> (<%= @booking_contact.use_plan.care_manager.office_name %>)</td>
  </tr>
</table>